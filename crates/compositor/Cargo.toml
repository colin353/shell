[package]
name = "compositor"
version = "0.1.0"
edition = "2021"

[dependencies]
emulator.path = "../emulator"
pty.path = "../pty"
libvim.path = "../libvim"
libshell.path = "../libshell"
protocol.path = "../protocol"
nix = { version = "0.29", features = ["fs"] }
libc = "0.2"
chrono = "0.4"
getrandom = "0.3"
regex = "1"
unicode-width = "0.2"
[dev-dependencies]
tempfile = "3.10"
