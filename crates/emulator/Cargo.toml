[package]
name = "emulator"
version = "0.1.0"
edition = "2021"

[dependencies]
alacritty_terminal = "0.25"
unicode-width = "0.2"

[dev-dependencies]
pty = { path = "../pty" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serial_test = "3"
